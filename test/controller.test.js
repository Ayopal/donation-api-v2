const request  = require('supertest')
const { createSendToken } = require('../utils/createSendToken')


describe('Create-Send Token', () => {
    it('Should return response with cookie, status, and data properties', () => {
        
    })
})